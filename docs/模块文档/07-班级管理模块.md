# 班级管理模块

> **文档版本**：1.1.0
> **更新时间**：2026-03-15

---

## 一、模块概述

管理幼儿园班级信息，包括班级的增删改查、教师分配等功能。支持一个班级分配多名教师，通过 `busi_teacher_class` 关联表实现多教师支持。

---

## 二、代码文件路径

### 后端代码

| 文件 | 路径 | 说明 |
|------|------|------|
| BusiClassController | [demo/src/main/java/com/kds/controller/busi/BusiClassController.java](demo/src/main/java/com/kds/controller/busi/BusiClassController.java) | 班级管理控制器 |
| BusiClassService | [demo/src/main/java/com/kds/service/BusiClassService.java](demo/src/main/java/com/kds/service/BusiClassService.java) | 班级服务接口 |
| BusiClassServiceImpl | [demo/src/main/java/com/kds/service/impl/BusiClassServiceImpl.java](demo/src/main/java/com/kds/service/impl/BusiClassServiceImpl.java) | 班级服务实现 |
| BusiClassMapper | [demo/src/main/java/com/kds/mapper/BusiClassMapper.java](demo/src/main/java/com/kds/mapper/BusiClassMapper.java) | 班级数据访问 |
| BusiClass | [demo/src/main/java/com/kds/model/entity/BusiClass.java](demo/src/main/java/com/kds/model/entity/BusiClass.java) | 班级实体类 |
| ClassVO | [demo/src/main/java/com/kds/model/vo/ClassVO.java](demo/src/main/java/com/kds/model/vo/ClassVO.java) | 班级视图对象 |
| ClassFormDTO | [demo/src/main/java/com/kds/model/dto/ClassFormDTO.java](demo/src/main/java/com/kds/model/dto/ClassFormDTO.java) | 班级表单DTO |
| ClassQueryDTO | [demo/src/main/java/com/kds/model/dto/ClassQueryDTO.java](demo/src/main/java/com/kds/model/dto/ClassQueryDTO.java) | 班级查询DTO |

### 前端代码

| 文件 | 路径 | 说明 |
|------|------|------|
| Class.vue | [frontend/src/views/busi/Class.vue](frontend/src/views/busi/Class.vue) | 班级管理页面 |

---

## 三、技术实现方案

### 3.1 核心功能点

| 功能 | 实现方法 | 说明 |
|------|----------|------|
| 班级分页查询 | `getClassPage()` | 支持按班级名称和年级搜索，关联展示任课教师列表 |
| 新增班级 | `addClass()` | 需校验班级名称唯一性 |
| 更新班级 | `updateClass()` | 支持修改班级信息 |
| 删除班级 | `deleteClass()` | 逻辑删除，同时删除教师班级关联 |
| 教师分配 | `assignTeacher()` | 分配/修改班级任课教师，支持多教师 |

### 3.2 核心代码逻辑

#### 3.2.1 分页查询班级并关联教师列表

```java
@Override
public Page<ClassVO> getClassPage(ClassQueryDTO queryDTO) {
    Page<BusiClass> pageParam = new Page<>(queryDTO.getPage(), queryDTO.getSize());

    LambdaQueryWrapper<BusiClass> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(BusiClass::getIsDeleted, 0);
    if (StringUtils.hasText(queryDTO.getClassName())) {
        wrapper.like(BusiClass::getClassName, queryDTO.getClassName());
    }
    if (queryDTO.getGrade() != null) {
        wrapper.eq(BusiClass::getGrade, queryDTO.getGrade());
    }

    // 数据隔离：管理员/园长可查看所有班级，其他角色只查看关联班级
    List<Long> classIds = LoginHelper.getClassIds();
    if (classIds != null && !classIds.isEmpty()) {
        wrapper.in(BusiClass::getId, classIds);
    } else if (!LoginHelper.isAdminOrDirector() && !LoginHelper.isParent()) {
        wrapper.eq(BusiClass::getId, -1L);
    }

    wrapper.orderByDesc(BusiClass::getCreateTime);
    Page<BusiClass> classPage = this.page(pageParam, wrapper);

    // 批量查询班级教师关联
    List<Long> classIdsList = classPage.getRecords().stream()
            .map(BusiClass::getId).collect(Collectors.toList());

    Map<Long, List<BusiTeacherClass>> teacherClassMap;
    if (!classIdsList.isEmpty()) {
        LambdaQueryWrapper<BusiTeacherClass> tcWrapper = new LambdaQueryWrapper<>();
        tcWrapper.in(BusiTeacherClass::getClassId, classIdsList);
        List<BusiTeacherClass> teacherClasses = busiTeacherClassMapper.selectList(tcWrapper);
        teacherClassMap = teacherClasses.stream()
                .collect(Collectors.groupingBy(BusiTeacherClass::getClassId));
    }

    // 实体转VO，并组装教师列表
    List<ClassVO> voList = classPage.getRecords().stream().map(cls -> {
        ClassVO vo = new ClassVO();
        BeanUtils.copyProperties(cls, vo);

        List<BusiTeacherClass> teacherClassList = teacherClassMap.get(cls.getId());
        List<TeacherSimpleVO> teachers = new ArrayList<>();
        if (teacherClassList != null) {
            for (BusiTeacherClass tc : teacherClassList) {
                SysUser teacherUser = teacherMap.get(tc.getTeacherUserId());
                if (teacherUser != null) {
                    TeacherSimpleVO teacherVO = new TeacherSimpleVO();
                    teacherVO.setId(teacherUser.getId());
                    teacherVO.setRealName(teacherUser.getRealName());
                    teacherVO.setIsHeadTeacher(tc.getIsHeadTeacher());
                    teachers.add(teacherVO);
                }
            }
        }
        vo.setTeachers(teachers);
        return vo;
    }).collect(Collectors.toList());

    return voPage;
}
```

#### 3.2.2 新增班级

```java
@Override
public void addClass(ClassFormDTO formDTO) {
    // 校验班级名称唯一性
    checkClassNameUnique(formDTO.getClassName(), null);

    // 复制属性到实体
    BusiClass busiClass = new BusiClass();
    BeanUtils.copyProperties(formDTO, busiClass);

    // 保存数据
    this.save(busiClass);
}
```

#### 3.2.3 校验班级名称唯一性

```java
private void checkClassNameUnique(String className, Long excludeId) {
    LambdaQueryWrapper<BusiClass> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(BusiClass::getClassName, className)
           .eq(BusiClass::getIsDeleted, 0);
    if (excludeId != null) {
        wrapper.ne(BusiClass::getId, excludeId);
    }
    if (this.count(wrapper) > 0) {
        throw new BusinessException("该班级名称已存在！");
    }
}
```

---

## 四、核心 API 接口

### 4.1 班级管理接口

| 接口路径 | 请求方式 | 功能说明 |
|---------|---------|---------|
| `/api/busi/class/page` | GET | 班级分页列表（带数据隔离） |
| `/api/busi/class/add` | POST | 新增班级 |
| `/api/busi/class/update` | POST | 更新班级 |
| `/api/busi/class/delete/{id}` | POST | 删除班级 |

### 4.2 教师班级关联接口

| 接口路径 | 请求方式 | 功能说明 |
|---------|---------|---------|
| `/api/busi/teacher-class/assign` | POST | 分配教师到班级 |
| `/api/system/user/teacher-list` | GET | 获取教师列表（下拉框用） |

---

## 五、数据模型

### 5.1 实体类

```java
public class BusiClass {
    private Long id;           // 主键ID
    private String className; // 班级名称
    private Integer grade;    // 年级：1-托班 2-小班 3-中班 4-大班
    private LocalDateTime createTime;
    private LocalDateTime updateTime;
    private Integer isDeleted; // 逻辑删除
}
```

### 5.2 视图对象

```java
public class ClassVO {
    private Long id;
    private String className;
    private Integer grade;
    private LocalDateTime createTime;
    private List<TeacherSimpleVO> teachers; // 任课教师列表
}
```

### 5.3 教师简单视图对象

```java
public class TeacherSimpleVO {
    private Long id;              // 教师用户ID
    private String realName;      // 教师姓名
    private Integer isHeadTeacher; // 是否为班主任：0-否 1-是
}
```

### 5.4 表结构

```sql
-- 班级信息表
CREATE TABLE `busi_class` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `class_name` varchar(50) NOT NULL COMMENT '班级名称(如：大一班)',
  `grade` tinyint NOT NULL COMMENT '年级：1-托班 2-小班 3-中班 4-大班',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` tinyint DEFAULT '0' COMMENT '逻辑删除：0-未删除 1-已删除',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='班级信息表';

-- 教师任教班级分配表
CREATE TABLE `busi_teacher_class` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `teacher_user_id` bigint NOT NULL COMMENT '教师用户ID',
  `class_id` bigint NOT NULL COMMENT '班级ID',
  `is_head_teacher` tinyint DEFAULT '0' COMMENT '是否为该班班主任：0-否 1-是',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '分配时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_teacher_class` (`teacher_user_id`, `class_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师任教班级分配表';
```

---

## 六、年级说明

| 值 | 说明 |
|----|------|
| 1 | 托班 |
| 2 | 小班 |
| 3 | 中班 |
| 4 | 大班 |

---

## 七、数据隔离功能

班级管理模块已集成数据隔离功能，根据用户角色控制数据可见性：

| 角色 | 数据范围 | 说明 |
|------|----------|------|
| 管理员 (ADMIN) | 全园数据 | 可查看所有班级 |
| 园长 (DIRECTOR) | 全园数据 | 可查看所有班级 |
| 班主任 (HEAD_TEACHER) | 本班数据 | 可查看任教的班级 |
| 普通教师 (TEACHER) | 本班数据 | 可查看所在班级 |
| 家长 (PARENT) | 绑定关系 | 暂不显示班级列表 |

### 实现方式

数据隔离通过以下组件实现：

1. **LoginHelper** - 获取当前用户角色和数据范围
2. **CustomDataPermissionHandler** - MyBatis Plus 数据权限处理器
3. **MybatisPlusConfig** - 配置数据权限拦截器

---

## 八、后续扩展

### 8.1 删除班级时的关联检查

在 `deleteClass()` 方法中，已添加删除教师班级关联的逻辑：

```java
@Override
public void deleteClass(Long id) {
    // 删除班级时，同时删除教师关联
    LambdaQueryWrapper<BusiTeacherClass> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(BusiTeacherClass::getClassId, id);
    busiTeacherClassMapper.delete(wrapper);

    // 逻辑删除班级
    this.removeById(id);
}
```

---

*文档生成时间：2026-03-15*
