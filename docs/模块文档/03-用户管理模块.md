# 用户管理模块

> **文档版本**：1.0.0
> **更新时间**：2026-03-15

---

## 一、模块概述

管理系统内部用户（教职工、家长），支持用户的增删改查、状态启用/禁用。

---

## 二、代码文件路径

### 后端代码

| 文件 | 路径 | 说明 |
|------|------|------|
| UserController | [demo/src/main/java/com/kds/controller/system/UserController.java](demo/src/main/java/com/kds/controller/system/UserController.java) | 用户管理控制器 |
| SysUserService | [demo/src/main/java/com/kds/service/SysUserService.java](demo/src/main/java/com/kds/service/SysUserService.java) | 用户服务接口 |
| SysUserServiceImpl | [demo/src/main/java/com/kds/service/impl/SysUserServiceImpl.java](demo/src/main/java/com/kds/service/impl/SysUserServiceImpl.java) | 用户服务实现 |
| SysUserMapper | [demo/src/main/java/com/kds/mapper/SysUserMapper.java](demo/src/main/java/com/kds/mapper/SysUserMapper.java) | 用户数据访问 |
| SysUser | [demo/src/main/java/com/kds/model/entity/SysUser.java](demo/src/main/java/com/kds/model/entity/SysUser.java) | 用户实体类 |
| UserVO | [demo/src/main/java/com/kds/model/vo/UserVO.java](demo/src/main/java/com/kds/model/vo/UserVO.java) | 用户视图对象 |
| UserFormDTO | [demo/src/main/java/com/kds/model/dto/UserFormDTO.java](demo/src/main/java/com/kds/model/dto/UserFormDTO.java) | 用户表单DTO |
| UserQueryDTO | [demo/src/main/java/com/kds/model/dto/UserQueryDTO.java](demo/src/main/java/com/kds/model/dto/UserQueryDTO.java) | 用户查询DTO |

### 前端代码

| 文件 | 路径 | 说明 |
|------|------|------|
| User.vue | [frontend/src/views/system/User.vue](frontend/src/views/system/User.vue) | 用户管理页面 |

---

## 三、技术实现方案

### 3.1 核心功能点

| 功能 | 实现方法 | 说明 |
|------|----------|------|
| 用户分页查询 | `getUserPage()` | 支持按姓名、手机号模糊搜索 |
| 新增用户 | `addUser()` | 需校验用户名唯一性，默认密码123456 |
| 更新用户 | `updateUser()` | 可更新密码、角色等信息 |
| 删除用户 | `deleteUser()` | 逻辑删除，设置 is_deleted=1 |
| 状态管理 | `updateById()` | 启用/禁用账号 |
| 获取教师列表 | `getTeacherList()` | 用于下拉框选择班主任 |

### 3.2 核心代码逻辑

#### 3.2.1 分页查询用户

```java
@Override
public Page<UserVO> getUserPage(UserQueryDTO queryDTO) {
    Page<SysUser> pageParam = new Page<>(queryDTO.getPage(), queryDTO.getSize());
    LambdaQueryWrapper<SysUser> wrapper = new LambdaQueryWrapper<>();

    wrapper.eq(SysUser::getIsDeleted, 0);
    if (StringUtils.hasText(queryDTO.getRealName())) {
        wrapper.like(SysUser::getRealName, queryDTO.getRealName());
    }
    if (StringUtils.hasText(queryDTO.getUsername())) {
        wrapper.like(SysUser::getUsername, queryDTO.getUsername());
    }
    wrapper.orderByDesc(SysUser::getCreateTime);

    Page<SysUser> userPage = this.page(pageParam, wrapper);

    // 实体转VO，并组装角色名称
    List<UserVO> voList = userPage.getRecords().stream().map(user -> {
        UserVO vo = new UserVO();
        BeanUtils.copyProperties(user, vo);

        // 查询用户关联角色
        SysUserRole userRole = sysUserRoleMapper.selectOne(
            new LambdaQueryWrapper<SysUserRole>().eq(SysUserRole::getUserId, user.getId()).last("LIMIT 1")
        );
        if (userRole != null) {
            vo.setRoleId(userRole.getRoleId());
            SysRole role = sysRoleMapper.selectById(userRole.getRoleId());
            vo.setRoleName(role != null ? role.getRoleName() : "未知");
        }
        return vo;
    }).collect(Collectors.toList());

    Page<UserVO> voPage = new Page<>(userPage.getCurrent(), userPage.getSize(), userPage.getTotal());
    voPage.setRecords(voList);
    return voPage;
}
```

#### 3.2.2 新增用户

```java
@Override
@Transactional(rollbackFor = Exception.class)
public void addUser(UserFormDTO formDTO) {
    // 校验用户名唯一性
    checkUsernameUnique(formDTO.getUsername(), null);

    SysUser user = new SysUser();
    BeanUtils.copyProperties(formDTO, user);

    // 设置默认密码
    if (!StringUtils.hasText(user.getPassword())) {
        user.setPassword("123456");
    }

    // 根据角色ID判断用户类型
    user.setUserType(formDTO.getRoleId() == 6 ? 2 : 1);
    this.save(user);

    // 保存用户角色关联
    SysUserRole userRole = new SysUserRole();
    userRole.setUserId(user.getId());
    userRole.setRoleId(formDTO.getRoleId());
    sysUserRoleMapper.insert(userRole);
}
```

#### 3.2.3 删除用户

```java
@Override
public void deleteUser(Long id) {
    // 使用 UpdateWrapper 绕过 MyBatis Plus 的逻辑删除自动过滤
    UpdateWrapper<SysUser> wrapper = new UpdateWrapper<>();
    wrapper.eq("id", id).set("is_deleted", 1);
    this.update(null, wrapper);
}
```

---

## 四、核心 API 接口

| 接口路径 | 请求方式 | 功能说明 |
|---------|---------|---------|
| `/api/system/user/page` | GET | 用户分页列表 |
| `/api/system/user/add` | POST | 新增用户 |
| `/api/system/user/update` | POST | 更新用户 |
| `/api/system/user/status` | POST | 更新用户状态 |
| `/api/system/user/delete/{id}` | POST | 删除用户 |
| `/api/system/user/teacher-list` | GET | 获取教师列表（下拉框用） |

---

## 五、数据模型

### 5.1 实体类

```java
public class SysUser {
    private Long id;           // 主键ID
    private String username;   // 登录账号（手机号）
    private String password;   // 登录密码
    private String realName;  // 真实姓名
    private String phone;      // 联系电话
    private Integer userType;  // 用户类型：1-教职工 2-家长
    private Integer status;    // 账号状态：0-禁用 1-正常
    private LocalDateTime createTime;
    private LocalDateTime updateTime;
    private Integer isDeleted; // 逻辑删除标志
}
```

### 5.2 视图对象

```java
public class UserVO {
    private Long id;
    private String realName;
    private String username;
    private Integer userType;
    private Long roleId;
    private String roleName;
    private Integer status;
    private LocalDateTime createTime;
}
```

---

## 六、前端页面功能

- **可爱风格的幼儿园主题 UI**
- **表格展示**：姓名、手机号、用户类型、角色、状态、创建时间
- **快捷操作**：启用/禁用开关、编辑、删除
- **搜索表单**：按姓名、手机号筛选
- **分页组件**：支持10/20/50/100条每页

---

## 七、用户类型说明

| 值 | 说明 |
|----|------|
| 1 | 教职工 |
| 2 | 家长 |

---

*文档生成时间：2026-03-15*
